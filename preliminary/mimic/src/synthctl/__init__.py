"""Phase 2: the latent synthetic-control estimator, its baselines, and its
evaluation.

Submodules are imported explicitly (`from src.synthctl.fit import Panel`) rather
than re-exported here, so importing the package costs nothing and pulls in no
sklearn/scipy.  See MODULE.md.
"""
